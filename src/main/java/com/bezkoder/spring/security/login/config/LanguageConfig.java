package com.bezkoder.spring.security.login.config;

import java.util.Arrays;
import java.util.List;
import java.util.Locale;

public class LanguageConfig {
    public static final List<Locale> LOCALS = Arrays.asList(
            new Locale("en"),
            new Locale("ar")
            );
}
